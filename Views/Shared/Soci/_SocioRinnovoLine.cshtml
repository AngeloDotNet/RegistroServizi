@model SocioRinnovoViewModel
<hr>
<div class="row">
    <div class="col-md-2 testo-tabella-bt">@Model.Anno</div>
    <div class="col-md-2 testo-tabella-bt">@Model.Quota</div>
    <div class="col-md-2 testo-tabella-bt">@Model.DataRinnovo</div>
    <div class="col-md-3"></div>
    <div class="col-md-3 d-flex align-items-center">
        <a class="btn btn-outline-secondary ml-3" asp-controller="SociRinnovi" asp-action="Edit" asp-route-id="@Model.Id"><i class="fas fa-edit"></i> Modifica</a>&nbsp;
        <button class="btn btn-outline-danger" type="button" data-toggle="modal" data-target="#delete-modal"><i class="fas fa-trash-alt"></i> Elimina</button>
    </div>
</div>
<form method="post" asp-controller="SociRinnovi" asp-action="Delete" asp-route-id="@Model.Id">
        <input type="hidden" asp-for="Id">
        <div id="delete-modal" class="modal fade" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="delete-modal-title" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="delete-modal-title">Conferma eliminazione</h5>
                        <button class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <p>Si sta per eliminare il rinnovo di un socio e tutti i suoi dati andranno persi. Confermi l'eliminazione?</p>
                    </div>
                    <div class="modal-footer justify-content-between">
                        <button class="btn btn-light" type="button" data-dismiss="modal">Annulla</button>
                        <button type="submit" class="btn btn-danger">Conferma</button>
                    </div>
                </div>
            </div>
        </div>
    </form>